<input confId="confchain_{chainN}_{disposition}" disposition="{disposition}" type="text" name="conf_{fieldName}"
 id="conf_{fieldName}" class="confirmation-input input-block-level" placeholder="{placeHolder}" chainN="{chainN}"
  said="{said}" dF="{dF}" rep="{rep}" globalConf="{globalConf}" fieldname="{fieldName}" value="{default}" mediaid="{mediaId}" maxbound="{maxbound}" {disabled} />
