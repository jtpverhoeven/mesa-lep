<input confId="confchain_{chainN}_{disposition}" disposition="{disposition}" type="text" name="conf_{fieldName}"
 id="conf_{fieldName}" class="confirmation-input input-block-level materialValue" placeholder="{placeHolder}" chainN="{chainN}"
    said="{said}" dF="{dF}" rep="{rep}" globalConf="{globalConf}" fieldname="{fieldName}" value="{default}" mediaid="{mediaId}" maxbound="{maxbound}" data-acceptable-range="{acceptable_range}" data-out-of-date-here="{out_of_date_here}" {disabled} />

{explanation_button}