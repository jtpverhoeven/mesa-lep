@extends('layouts.app')
@section('title', 'MESA | Standaard bemonsterprocedures')
@section('content')
    <div class="page-heading"><div><div class="eyebrow">Beheer / Analysestroom</div><h1>Standaard bemonsterprocedures</h1></div><a class="button" href="{{ route('sample-procedures.index') }}">Terug naar overzicht</a></div>
    <form method="POST" action="{{ route('sample-procedures.defaults.update') }}">@csrf @method('PUT')<fieldset class="form-section"><legend>Standaarden</legend><div class="form-grid">@foreach(['default_proc' => ['MESA_STD_SMPL_METHOD', 'Standaard bemonsterprocedure'], 'default_leg_proc' => ['MESA_STD_LEGSMPL_METHOD', 'Standaard bemonsterprocedure legionella'], 'default_rodac_proc' => ['MESA_STD_RODACSMPL_METHOD', 'Standaard bemonsterprocedure RODAC']] as $name => [$key, $label])<div class="field"><label for="{{ $name }}">{{ $label }}</label><select id="{{ $name }}" name="{{ $name }}">@php($selected = old($name, $defaults[$key] ?? 0))<option value="0" @selected((int) $selected === 0)>Onbekend</option>@foreach($procedures as $procedure)<option value="{{ $procedure->id }}" @selected((int) $selected === $procedure->id)>{{ $procedure->name }}</option>@endforeach</select></div>@endforeach</div></fieldset><div class="form-actions"><button class="button primary" type="submit">Opslaan</button><a class="button" href="{{ route('sample-procedures.index') }}">Annuleren</a></div></form>
@endsection